import { PageHeading } from '../../components/PageHeading';
import { PagePlaceholder } from '../../components/PagePlaceholder';

/** Nạp danh sách từ Excel (UC-24) — khung màn hình, nội dung dựng ở task riêng. */
export default function MembersImportPage() {
  return (
    <>
      <PageHeading title="Nạp danh sách từ file Excel" />
      <PagePlaceholder screen="Nạp danh sách từ file Excel" />
    </>
  );
}
