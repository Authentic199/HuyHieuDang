import { PageHeading } from '../../components/PageHeading';
import { PagePlaceholder } from '../../components/PagePlaceholder';

/** Đợt trao huy hiệu — khung màn hình, nội dung dựng ở task riêng. */
export default function PeriodsPage() {
  return (
    <>
      <PageHeading title="Đợt trao huy hiệu" />
      <PagePlaceholder screen="Đợt trao huy hiệu" />
    </>
  );
}
