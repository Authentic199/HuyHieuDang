import { PageHeading } from '../../components/PageHeading';
import { PagePlaceholder } from '../../components/PagePlaceholder';

/** Chi tiết một đợt (UC-34) — khung màn hình, nội dung dựng ở task riêng. */
export default function PeriodDetailPage() {
  return (
    <>
      <PageHeading title="Chi tiết đợt trao huy hiệu" />
      <PagePlaceholder screen="Chi tiết đợt trao huy hiệu" />
    </>
  );
}
